[project]
name = "turtlebot"
version = "0.1.0"
description = "TurtleBot4 UDP teleop and telemetry PC-side tools"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.0",
    "opencv-python>=4.10",
    "pyzbar>=0.1.9",
    "ultralytics>=8.4.60",
]

[dependency-groups]
dev = []
